# MCP Weather Tools - Документация

## Описание

Реализация MCP (Model Context Protocol) сервера для интеграции Yandex Weather API с AI агентом.

MCP позволяет агенту вызывать внешние инструменты (tools) для получения данных в реальном времени, расширяя его возможности за пределы текстовой генерации.

## Архитектура

```
┌─────────────────┐
│   Yandex GPT    │ ← LLM может вызывать tools
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│   MCP Server    │ ← Регистрирует и управляет инструментами
├─────────────────┤
│ - listTools()   │
│ - executeTool() │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ Weather Provider│ ← Вызывает Yandex Weather API
├─────────────────┤
│ - getWeather()  │
│ - getForecast() │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ Yandex Weather  │
│      API        │
└─────────────────┘
```

## Структура проекта

```
src/main/kotlin/kz/shprot/
├── mcp/
│   ├── McpServer.kt                    # MCP протокол (регистрация, выполнение)
│   ├── models/
│   │   ├── McpModels.kt                # Tool, ToolCall, ToolResult
│   │   └── WeatherModels.kt            # Модели для Weather API
│   └── providers/
│       └── YandexWeatherProvider.kt    # Клиент для Yandex Weather API
└── Main.kt                             # Интеграция (роуты /api/tools/*)
```

## Доступные инструменты

### 1. `get_current_weather`

Получить текущую погоду для указанной локации.

**Параметры:**
- `location` (string, обязательно) - Название города или локации

**Пример:**
```json
{
  "location": "Алматы"
}
```

**Результат:**
```
Текущая погода в Алматы:
🌡️ Температура: 15°C (ощущается как 13°C)
☁️ Условия: облачно с прояснениями
💧 Влажность: 65%
💨 Ветер: северо-западный, 3.5 м/с
🔽 Давление: 760 мм рт.ст.
```

### 2. `get_weather_forecast`

Получить прогноз погоды на несколько дней.

**Параметры:**
- `location` (string, обязательно) - Название города или локации
- `days` (integer, опционально, по умолчанию 3) - Количество дней прогноза (1-7)

**Пример:**
```json
{
  "location": "Москва",
  "days": 5
}
```

**Результат:**
```
Прогноз погоды для Москва на 5 дней:

📅 2024-01-15:
  🌞 Днем: 2°C
  🌙 Ночью: -3°C
  ☁️ Условия: облачно
  💧 Влажность: 78%
  🌧️ Осадки: 0.5 мм

...
```

## API Endpoints

### `GET /api/tools`

Получить список всех доступных инструментов.

**Response:**
```json
{
  "tools": [
    {
      "name": "get_current_weather",
      "description": "Получить текущую погоду для указанной локации...",
      "inputSchema": {
        "type": "object",
        "properties": {
          "location": {
            "type": "string",
            "description": "Название города или локации"
          }
        },
        "required": ["location"]
      }
    }
  ]
}
```

### `POST /api/tools/execute`

Прямое выполнение инструмента (для тестирования).

**Request:**
```json
{
  "name": "get_current_weather",
  "arguments": {
    "location": "Алматы"
  }
}
```

**Response:**
```json
{
  "result": "Текущая погода в Алматы:\n🌡️ Температура: 15°C...",
  "isError": false
}
```

## Настройка

### 1. Получение API ключа Yandex Weather

1. Перейдите на https://yandex.ru/dev/weather/
2. Зарегистрируйтесь или войдите
3. Создайте новое приложение
4. Скопируйте API ключ

### 2. Настройка переменных окружения

Создайте файл `.env` (или настройте в IDE):

```bash
# Обязательные переменные
YANDEX_API_KEY=your_yandex_api_key_here
YANDEX_FOLDER_ID=your_yandex_folder_id_here

# Для MCP Weather Tools (опционально)
YANDEX_WEATHER_API_KEY=your_yandex_weather_api_key_here
```

**Примечание:** Если `YANDEX_WEATHER_API_KEY` не указан, MCP сервер не будет инициализирован, но основной функционал продолжит работать.

### 3. Запуск

```bash
# Сборка
./gradlew build

# Запуск
./gradlew run
```

При запуске вы увидите:
```
=== Локальный сервер для общения с Yandex LLM ===
База данных: chats.db
Модель: yandexgpt
JSON Schema: включена
Multi-Agent система: включена
MCP Weather Tools: включены  ← Статус MCP
Сервер запускается на http://localhost:8080
```

## Тестирование

### Способ 1: Прямой вызов через API

```bash
# Получить список инструментов
curl http://localhost:8080/api/tools

# Выполнить инструмент
curl -X POST http://localhost:8080/api/tools/execute \
  -H "Content-Type: application/json" \
  -d '{
    "name": "get_current_weather",
    "arguments": {
      "location": "Алматы"
    }
  }'
```

### Способ 2: Через веб интерфейс

1. Откройте http://localhost:8080
2. Создайте новый чат
3. Спросите у агента:
   - "Какая погода в Алматы?"
   - "Покажи прогноз погоды в Москве на неделю"
   - "Какая температура в Париже?"

**Важно:** На данный момент автоматический Function Calling еще не полностью интегрирован. Агент использует прямые вызовы к API, но в будущих версиях будет автоматически вызывать tools при необходимости.

### Способ 3: Через curl с JSON

```bash
curl -X POST http://localhost:8080/api/tools/execute \
  -H "Content-Type: application/json" \
  -d @- << 'EOF'
{
  "name": "get_weather_forecast",
  "arguments": {
    "location": "Москва",
    "days": 3
  }
}
EOF
```

## Технические детали

### Геокодирование

Для получения координат по названию города используется OpenStreetMap Nominatim (бесплатный, без API ключа). Это позволяет поддерживать любые локации по всему миру.

### Перевод погодных условий

Все погодные условия автоматически переводятся на русский язык:
- `clear` → "ясно"
- `partly-cloudy` → "малооблачно"
- `rain` → "дождь"
- и т.д.

### Обработка ошибок

MCP сервер корректно обрабатывает ошибки:
- Неизвестная локация → "Не удалось найти координаты для локации: X"
- Ошибка API → "Ошибка получения погоды: [детали]"
- Отсутствие WEATHER_API_KEY → 503 Service Unavailable

## Расширение

### Добавление нового инструмента

1. **Создайте провайдер** (например, `CurrencyProvider.kt`):
```kotlin
class CurrencyProvider(private val apiKey: String) {
    suspend fun getExchangeRate(from: String, to: String): Result<Double> {
        // Реализация
    }
}
```

2. **Зарегистрируйте tool в McpServer**:
```kotlin
fun listTools(): List<Tool> = listOf(
    // ... существующие tools
    Tool(
        name = "get_exchange_rate",
        description = "Получить курс обмена валют",
        inputSchema = buildJsonObject {
            // JSON Schema
        }
    )
)
```

3. **Добавьте обработчик**:
```kotlin
suspend fun executeTool(name: String, arguments: JsonObject): ToolExecutionResponse {
    return when (name) {
        // ... существующие tools
        "get_exchange_rate" -> executeGetExchangeRate(arguments)
        else -> ToolExecutionResponse("Tool not found", isError = true)
    }
}
```

## Полезные ссылки

- [Yandex Weather API](https://yandex.ru/dev/weather/)
- [OpenStreetMap Nominatim](https://nominatim.org/)
- [MCP Specification](https://modelcontextprotocol.io/)
- [Yandex Cloud Foundation Models](https://cloud.yandex.ru/docs/foundation-models/)

## Troubleshooting

**Проблема:** `MCP Server not available`
- **Решение:** Убедитесь, что установлена переменная `YANDEX_WEATHER_API_KEY`

**Проблема:** `Не удалось найти координаты для локации`
- **Решение:** Попробуйте указать город на английском ("Moscow" вместо "Москва")

**Проблема:** Медленный ответ
- **Решение:** Это нормально при первом запросе к новой локации (геокодирование + API вызов)

## Будущие улучшения

- [ ] Автоматический Function Calling (LLM сам решает когда вызывать tools)
- [ ] Кэширование координат локаций
- [ ] Кэширование погодных данных (TTL 10-15 минут)
- [ ] Больше погодных метрик (UV индекс, видимость, etc)
- [ ] Поддержка других weather API (OpenWeatherMap, Weather.com)
- [ ] Расширенные инструменты (погода по координатам, радар осадков)
