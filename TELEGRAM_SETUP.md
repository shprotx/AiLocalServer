# Настройка Telegram MCP сервера

## Получение API credentials

Чтобы использовать Telegram MCP, вам нужны `TG_APP_ID` и `TG_API_HASH`:

### Шаг 1: Получение API ID и API Hash

1. Перейдите на https://my.telegram.org/auth
2. Войдите с помощью вашего номера телефона
3. Перейдите в раздел "API development tools"
4. Создайте новое приложение:
   - **App title**: AiLocalServer (или любое другое название)
   - **Short name**: aiserver (или любое другое)
   - **Platform**: Desktop
   - **Description**: Опционально
5. После создания вы получите:
   - **App api_id** - это ваш `TG_APP_ID`
   - **App api_hash** - это ваш `TG_API_HASH`

### Шаг 2: Настройка mcp-servers.json

Откройте файл `mcp-servers.json` и замените placeholder'ы на ваши реальные значения:

```json
{
  "mcpServers": {
    "telegram": {
      "type": "stdio",
      "command": "/opt/homebrew/bin/telegram-mcp",
      "args": [],
      "env": {
        "TG_APP_ID": "12345678",           // ← Замените на ваш App api_id
        "TG_API_HASH": "abcdef1234567890"  // ← Замените на ваш App api_hash
      },
      "description": "Telegram MCP сервер для работы с чатами, каналами и сообщениями"
    }
  }
}
```

### Шаг 3: Запуск и тестирование

1. Перезапустите сервер через IntelliJ IDEA (Run Server)
2. В логах вы должны увидеть:
   ```
   🔌 Starting MCP server 'telegram'...
      Command: /opt/homebrew/bin/telegram-mcp
      Environment variables: TG_APP_ID, TG_API_HASH
      PID: xxxxx
   ✅ Server 'telegram' ready
   ```

3. Протестируйте через веб-интерфейс:
   - "Сколько у меня чатов в Telegram?"
   - "Какие посты были в канале Mobile Developer за последние сутки?"
   - "Покажи мои непрочитанные сообщения"

## Доступные инструменты

Telegram MCP сервер предоставляет следующие инструменты:

### 1. `tg_me`
Получить информацию о текущем аккаунте

### 2. `tg_dialogs`
Получить список диалогов с фильтром по непрочитанным

**Параметры:**
- `unread_only` (boolean, опционально) - показать только непрочитанные

### 3. `tg_dialog`
Получить сообщения из конкретного диалога

**Параметры:**
- `dialog_id` (string) - ID диалога
- `limit` (number, опционально) - количество сообщений (по умолчанию 20)

### 4. `tg_read`
Отметить диалог как прочитанный

**Параметры:**
- `dialog_id` (string) - ID диалога

### 5. `tg_send`
Отправить черновик сообщения

**Параметры:**
- `dialog_id` (string) - ID диалога
- `message` (string) - текст сообщения

## Примеры использования

**Простой запрос:**
- "Сколько у меня чатов?" → используется `tg_dialogs`
- "Покажи информацию о моем аккаунте" → используется `tg_me`

**Работа с каналами:**
- "Какие посты в канале X за последние сутки?" → используется `tg_dialogs` + `tg_dialog`
- "Есть ли новые сообщения в канале Mobile Developer?" → используется `tg_dialog`

**Отправка сообщений:**
- "Отправь сообщение 'Привет' в чат Y" → используется `tg_send`

## Troubleshooting

### Ошибка "Session not found"
Вам нужно авторизоваться через CLI:
```bash
telegram-mcp auth
```
Следуйте инструкциям и введите код подтверждения из Telegram.

### Ошибка "Invalid API credentials"
Проверьте, что вы правильно указали `TG_APP_ID` и `TG_API_HASH` в `mcp-servers.json`.

### MCP сервер не запускается
Проверьте логи сервера. Убедитесь, что:
- `telegram-mcp` установлен глобально (`npm install -g @chaindead/telegram-mcp`)
- Путь к команде правильный (`/opt/homebrew/bin/telegram-mcp`)
