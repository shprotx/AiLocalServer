# MCP Orchestrator - Тестовые Промпты

## Описание
MCP Orchestrator автоматически композирует несколько MCP инструментов для выполнения сложных задач.

**Доступные MCP серверы:**
- **fetch** - получение контента из интернета
- **filesystem** - работа с файлами в Downloads
- **weather** - получение погоды
- **telegram** - работа с Telegram

**Endpoint:** `POST /api/mcp-orchestrator`

**Request:**
```json
{
  "task": "ваша задача",
  "temperature": 0.6
}
```

---

## Тест 1: Fetch → Save (Простая цепочка)

**Промпт:**
```
Получи содержимое страницы https://kotlinlang.org/ и сохрани в файл kotlin-homepage.md
```

**Ожидаемое поведение:**
1. LLM вызывает `fetch` с URL
2. Получает контент в Markdown
3. Вызывает `write_file` для сохранения
4. Возвращает финальный ответ

**curl команда:**
```bash
curl -X POST http://localhost:8080/api/mcp-orchestrator \
  -H "Content-Type: application/json" \
  -d '{
    "task": "Получи содержимое страницы https://kotlinlang.org/ и сохрани в файл kotlin-homepage.md",
    "temperature": 0.6
  }'
```

---

## Тест 2: Fetch → Summarize → Save (С обработкой)

**Промпт:**
```
Получи главную страницу Wikipedia на русском (https://ru.wikipedia.org/), создай краткое резюме (3-5 предложений) о структуре сайта и сохрани резюме в файл wikipedia-summary.txt
```

**Ожидаемое поведение:**
1. LLM вызывает `fetch` для получения контента
2. LLM сама анализирует и создает резюме (без отдельного инструмента)
3. LLM вызывает `write_file` для сохранения резюме
4. Возвращает финальный ответ

**curl команда:**
```bash
curl -X POST http://localhost:8080/api/mcp-orchestrator \
  -H "Content-Type: application/json" \
  -d '{
    "task": "Получи главную страницу Wikipedia на русском (https://ru.wikipedia.org/), создай краткое резюме (3-5 предложений) о структуре сайта и сохрани резюме в файл wikipedia-summary.txt",
    "temperature": 0.6
  }'
```

---

## Тест 3: Weather → Save (Другие инструменты)

**Промпт:**
```
Получи текущую температуру для Москвы (широта 55.7558, долгота 37.6173) и сохрани в файл moscow-temperature.txt
```

**Ожидаемое поведение:**
1. LLM вызывает `get_current_temperature` с координатами Москвы
2. Получает температуру
3. Вызывает `write_file` для сохранения
4. Возвращает финальный ответ

**curl команда:**
```bash
curl -X POST http://localhost:8080/api/mcp-orchestrator \
  -H "Content-Type: application/json" \
  -d '{
    "task": "Получи текущую температуру для Москвы (широта 55.7558, долгота 37.6173) и сохрани в файл moscow-temperature.txt",
    "temperature": 0.6
  }'
```

---

## Тест 4: Множественные Fetch → Combine → Save (Длинная цепочка)

**Промпт:**
```
Получи главные страницы Kotlin (https://kotlinlang.org/) и Python (https://python.org/), сравни их (напиши 2-3 предложения о различиях) и сохрани сравнение в файл kotlin-vs-python.md
```

**Ожидаемое поведение:**
1. LLM вызывает `fetch` для Kotlin
2. LLM вызывает `fetch` для Python
3. LLM анализирует оба контента
4. LLM вызывает `write_file` для сохранения сравнения
5. Возвращает финальный ответ

**Ожидаемое количество итераций:** 4-5

**curl команда:**
```bash
curl -X POST http://localhost:8080/api/mcp-orchestrator \
  -H "Content-Type: application/json" \
  -d '{
    "task": "Получи главные страницы Kotlin (https://kotlinlang.org/) и Python (https://python.org/), сравни их (напиши 2-3 предложения о различиях) и сохрани сравнение в файл kotlin-vs-python.md",
    "temperature": 0.6
  }'
```

---

## Тест 5: Read → Process → Write (Filesystem операции)

**Промпт:**
```
Прочитай файл kotlin-homepage.md (если он существует), посчитай количество слов в нем и сохрани результат в файл word-count.txt
```

**Ожидаемое поведение:**
1. LLM вызывает `read_text_file` для чтения kotlin-homepage.md
2. LLM анализирует контент и считает слова
3. LLM вызывает `write_file` для сохранения результата
4. Возвращает финальный ответ

**Предварительное условие:** Сначала выполните Тест 1

**curl команда:**
```bash
curl -X POST http://localhost:8080/api/mcp-orchestrator \
  -H "Content-Type: application/json" \
  -d '{
    "task": "Прочитай файл kotlin-homepage.md, посчитай количество слов в нем и сохрани результат в файл word-count.txt",
    "temperature": 0.6
  }'
```

---

## Тест 6: Search Files → Read → Summarize (Сложная задача)

**Промпт:**
```
Найди все .txt файлы в Downloads, прочитай первый найденный файл и создай его краткое резюме (2-3 предложения) в файле summary-of-first-txt.md
```

**Ожидаемое поведение:**
1. LLM вызывает `search_files` для поиска .txt файлов
2. LLM вызывает `read_text_file` для чтения первого файла
3. LLM анализирует и создает резюме
4. LLM вызывает `write_file` для сохранения
5. Возвращает финальный ответ

**curl команда:**
```bash
curl -X POST http://localhost:8080/api/mcp-orchestrator \
  -H "Content-Type: application/json" \
  -d '{
    "task": "Найди все .txt файлы в Downloads, прочитай первый найденный файл и создай его краткое резюме (2-3 предложения) в файле summary-of-first-txt.md",
    "temperature": 0.6
  }'
```

---

## Тест 7: Telegram + Fetch + Save (Композиция разных серверов)

**Промпт:**
```
Получи список моих диалогов в Telegram, получи информацию о первом диалоге и сохрани ее в файл first-telegram-dialog.txt
```

**Ожидаемое поведение:**
1. LLM вызывает `tg_dialogs`
2. Получает список диалогов
3. LLM вызывает `tg_dialog` для первого диалога
4. LLM вызывает `write_file` для сохранения
5. Возвращает финальный ответ

**Предварительное условие:** Telegram MCP должен быть настроен и авторизован

**curl команда:**
```bash
curl -X POST http://localhost:8080/api/mcp-orchestrator \
  -H "Content-Type: application/json" \
  -d '{
    "task": "Получи список моих диалогов в Telegram, получи информацию о первом диалоге и сохрани ее в файл first-telegram-dialog.txt",
    "temperature": 0.6
  }'
```

---

## Как проверить результаты

После выполнения тестов проверьте:
1. **Логи сервера** - должны показывать каждый вызов MCP инструмента:
   ```
   🔧 Вызов MCP инструмента: fetch
      Параметры: {url=https://kotlinlang.org/}
   ✅ Результат: ...
   ```

2. **Файлы в Downloads** - все созданные файлы должны быть там

3. **Response JSON** - должен содержать:
   - `success: true`
   - `finalAnswer` - итоговый ответ LLM
   - `toolCalls` - массив всех вызовов инструментов
   - `iterations` - количество итераций

---

## Ожидаемая структура логов

```
🎯 Начинаем выполнение задачи через MCP Orchestrator
   Запрос: Получи содержимое страницы...

📍 Итерация 1/15
🤖 LLM ответ:
   TOOL_CALL: fetch
   PARAMETERS: {"url": "https://kotlinlang.org/"}
🔧 Вызов MCP инструмента: fetch
   Параметры: {url=https://kotlinlang.org/}
✅ Результат: # Kotlin Programming Language...

📍 Итерация 2/15
🤖 LLM ответ:
   TOOL_CALL: write_file
   PARAMETERS: {"path": "kotlin-homepage.md", "content": "..."}
🔧 Вызов MCP инструмента: write_file
   Параметры: {path=kotlin-homepage.md, content=...}
✅ Результат: File written successfully

📍 Итерация 3/15
🤖 LLM ответ:
   FINAL_ANSWER: Содержимое страницы Kotlin успешно сохранено в файл kotlin-homepage.md
✅ Финальный ответ получен
   Всего итераций: 3
   Всего вызовов MCP: 2
```

---

## Troubleshooting

**Проблема:** "Tool not found"
- **Решение:** Проверьте что MCP серверы запущены (`mcp-servers.json` правильно настроен)

**Проблема:** "Превышен лимит итераций"
- **Решение:** Увеличьте `maxIterations` в McpOrchestrator или упростите задачу

**Проблема:** LLM не вызывает инструменты
- **Решение:** Проверьте system prompt в McpOrchestrator, возможно нужно более явное указание формата

**Проблема:** Filesystem ошибка "Permission denied"
- **Решение:** Проверьте что путь `/Users/artur/Downloads` доступен и прописан в `mcp-servers.json`

---

## Дополнительно: Тест через Postman/Insomnia

1. Создайте новый POST запрос к `http://localhost:8080/api/mcp-orchestrator`
2. Headers: `Content-Type: application/json`
3. Body (raw JSON):
```json
{
  "task": "ваша задача здесь",
  "temperature": 0.6
}
```
4. Отправьте запрос
5. Проверьте response и логи сервера

---

## Результаты тестирования

После выполнения всех тестов у вас должно быть:
- ✅ 5-7 новых файлов в Downloads
- ✅ Логи показывают все вызовы MCP
- ✅ Все responses содержат `success: true`
- ✅ Количество итераций соответствует ожидаемому (2-5 для простых задач, 5-10 для сложных)
